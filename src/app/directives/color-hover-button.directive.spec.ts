import { ColorHoverButtonDirective } from './color-hover-button.directive';

describe('ColorHoverButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorHoverButtonDirective();
    expect(directive).toBeTruthy();
  });
});
